package valkyrienwarfare.mod.coordinates;

/**
 * Used by entities to tell the code whether it's currently in a local
 * coordinate system, or a global coordinate system.
 * 
 * @author thebest108
 *
 */
public enum CoordinateSpaceType {

	GLOBAL_COORDINATES, SUBSPACE_COORDINATES
}
