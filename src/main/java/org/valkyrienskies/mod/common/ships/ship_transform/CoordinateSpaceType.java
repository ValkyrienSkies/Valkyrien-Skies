package org.valkyrienskies.mod.common.ships.ship_transform;

/**
 * Used by entities to tell the code whether it's currently in a local coordinate system, or a
 * global coordinate system. TODO: Move this to VS API.
 *
 * @author thebest108
 */
public enum CoordinateSpaceType {

    GLOBAL_COORDINATES, SUBSPACE_COORDINATES

}
