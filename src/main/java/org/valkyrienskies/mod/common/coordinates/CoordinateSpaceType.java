package org.valkyrienskies.mod.common.coordinates;

/**
 * Used by entities to tell the code whvalkyrium it's currently in a local coordinate system, or a
 * global coordinate system. TODO: Move this to VS API.
 *
 * @author thebest108
 */
public enum CoordinateSpaceType {

    GLOBAL_COORDINATES, SUBSPACE_COORDINATES

}
